apiVersion: apps/v1
kind: Deployment
metadata:
  name: angular-app
  namespace: angular-app   # Added namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: angular-app
  template:
    metadata:
      labels:
        app: angular-app
    spec:
      containers:
        - name: angular-container
          image: azizgmaty/mon-angular-app:22-cc65f5b3  # Updated image tag
          ports:
            - containerPort: 80
